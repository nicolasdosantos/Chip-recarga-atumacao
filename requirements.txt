# acesso ao Google Sheets
gspread==6.1.4
google-auth==2.35.0

# leitura/analise dos dados
pandas==2.2.2
openpyxl==3.1.5

# variaveis de ambiente (.env)
python-dotenv==1.0.1

# notificacao no discord:
# - Linux: usa "xdotool" e "wmctrl" (binarios de sistema, nao sao pacote
#   pip - instalar com "sudo apt install wmctrl xdotool")
# - Windows: usa as duas libs abaixo (so sao instaladas no Windows, por
#   causa do "; sys_platform == ..." no final da linha)
pyautogui==0.9.54; sys_platform == "win32"
pygetwindow==0.0.9; sys_platform == "win32"
